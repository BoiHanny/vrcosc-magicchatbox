<UserControl
    x:Class="vrcosc_magicchatbox.UI.Pages.Options.PulsoidSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:vrcosc_magicchatbox.ViewModels"
    xmlns:sections="clr-namespace:vrcosc_magicchatbox.ViewModels.Sections"
    d:DataContext="{d:DesignInstance Type=sections:PulsoidSectionViewModel,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UI/Resources/SharedConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0,5,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Background>
            <LinearGradientBrush SpreadMethod="Pad" StartPoint="1,1" EndPoint="0,0">
                <GradientStop Color="{StaticResource SurfaceGradientStartColor}" />
                <GradientStop Offset="1" Color="{StaticResource SurfaceGradientEndColor}" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid Grid.Row="0">

            <ToggleButton
                Width="Auto"
                Height="30"
                HorizontalAlignment="Left"
                FontSize="15"
                FontWeight="Bold"
                Foreground="{StaticResource TextSectionHeaderBrush}"
                IsChecked="{Binding AppSettings.Settings_HeartRate, Mode=TwoWay}"
                Style="{StaticResource ExpandCollapseToggleButtonStyle}"
                Tag="Pulsoid heart rate options"
                TextOptions.TextRenderingMode="ClearType" />

            <TextBlock
                x:Name="LearnMoreAboutHeartbtn"
                Width="auto"
                Margin="205,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Cursor="Hand"
                FontFamily="Albert Sans Thin"
                FontSize="12"
                Foreground="{StaticResource AccentTealBrush}"
                MouseUp="LearnMoreAboutHeartbtn_MouseUp"
                Text="Learn more"
                TextDecorations="Underline" />
        </Grid>
        <Grid
            Grid.Row="1"
            Height="Auto"
            Margin="34,10,0,0"
            Visibility="{Binding AppSettings.Settings_HeartRate, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
            <StackPanel>
                <Border
                    Width="500"
                    Height="auto"
                    Margin="0,0,10,15"
                    HorizontalAlignment="Left"
                    CornerRadius="5">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.3,0" EndPoint="0.5,1">
                            <GradientStop Color="{StaticResource AccentPulsoidColor}" />
                            <GradientStop Offset="1" Color="{StaticResource SurfaceMidGradientColor}" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect
                            BlurRadius="25"
                            Direction="-90"
                            Opacity="0.9"
                            RenderingBias="Performance"
                            ShadowDepth="5"
                            Color="{StaticResource AccentPulsoidColor}" />
                    </Border.Effect>
                    <Grid Margin="3">

                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Width="auto"
                                    Padding="5,5,0,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Foreground="Gold"
                                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                                    Text="15% OFF"
                                    TextWrapping="Wrap">
                                    <TextBlock.Effect>
                                        <DropShadowEffect
                                            BlurRadius="20"
                                            Opacity="1"
                                            RenderingBias="Performance"
                                            ShadowDepth="0"
                                            Color="BlueViolet" />
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock
                                    Width="auto"
                                    Padding="5,5,0,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Foreground="{StaticResource SurfaceDarkBrush}"
                                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                                    Text="every month on your"
                                    TextWrapping="Wrap">
                                    <TextBlock.Effect>
                                        <DropShadowEffect
                                            BlurRadius="20"
                                            Opacity="1"
                                            RenderingBias="Performance"
                                            ShadowDepth="0"
                                            Color="Purple" />
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock
                                    Width="auto"
                                    Padding="5,5,0,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Foreground="Gold"
                                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                                    Text="BRO PLAN"
                                    TextWrapping="Wrap">
                                    <TextBlock.Effect>
                                        <DropShadowEffect
                                            BlurRadius="20"
                                            Opacity="1"
                                            RenderingBias="Performance"
                                            ShadowDepth="0"
                                            Color="Gold" />
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock
                                Width="auto"
                                Padding="7,0,0,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Top"
                                Background="Transparent"
                                FontFamily="Albert Sans Thin"
                                FontSize="13"
                                FontStyle="Italic"
                                FontWeight="Medium"
                                Foreground="DarkGoldenrod"
                                RenderOptions.BitmapScalingMode="NearestNeighbor"
                                Text="Discount applies to new subscriptions only (cancel possible)"
                                TextWrapping="Wrap">
                                <TextBlock.Effect>
                                    <DropShadowEffect
                                        BlurRadius="7"
                                        Opacity="1"
                                        RenderingBias="Performance"
                                        ShadowDepth="0"
                                        Color="Purple" />
                                </TextBlock.Effect>
                            </TextBlock>


                        </StackPanel>
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <Button
                                Name="PulsoidLearnMoreAboutDiscount_btn"
                                Height="auto"
                                Margin="2,0,0,0"
                                VerticalAlignment="Stretch"
                                Command="{Binding PulsoidDiscountLearnMoreCommand}"
                                Opacity="0.8"
                                Style="{DynamicResource Status_Button_style}">
                                <Image
                                    Width="18"
                                    Margin="2"
                                    RenderOptions.BitmapScalingMode="HighQuality"
                                    Source="/Img/Icons/Info_ico.png" />
                            </Button>
                            <Button
                                Name="ApplyLinkPulsoid_btn"
                                Width="auto"
                                Height="auto"
                                Margin="2,0,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Stretch"
                                Command="{Binding PulsoidPricingCommand}"
                                Opacity="0.8"
                                Style="{DynamicResource Status_Button_style}">
                                <Image
                                    Width="25"
                                    Margin="2"
                                    RenderOptions.BitmapScalingMode="HighQuality"
                                    Source="/Img/Icons/Apply_ico.png" />
                            </Button>
                        </StackPanel>








                    </Grid>

                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <Button
                            x:Name="ConnectWithPulsoid"
                            Width="auto"
                            Height="30"
                            Padding="5"
                            Command="{Binding ConnectPulsoidCommand}"
                            Style="{StaticResource Status_Button_style}"
                            Visibility="{Binding Pulsoid.AuthConnected, Converter={StaticResource InverseBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image
                                            Width="21"
                                            Height="18"
                                            Margin="0,0,3,0"
                                            Source="/Img/Icons/Pulsoid.png" />
                                        <TextBlock
                                            Margin="0,-2,5,0"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            Text="Connect with Pulsoid" />
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                        <Button
                            x:Name="ConnectedWithPulsoid"
                            Width="auto"
                            Height="30"
                            Padding="5"
                            d:Visibility="Visible"
                            Command="{Binding ConnectPulsoidCommand}"
                            Style="{StaticResource Status_Button_style}"
                            Visibility="{Binding Pulsoid.AuthConnected, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image
                                            Width="15"
                                            Height="15"
                                            Margin="2,0,5,0"
                                            VerticalAlignment="Center"
                                            Source="/Img/Icons/yes.png" />
                                        <TextBlock
                                            Margin="0,-2,5,0"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            Text="Connected with Pulsoid" />
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                        <Button
                            x:Name="DisconnectPulsoid"
                            Grid.Row="0"
                            Grid.Column="1"
                            Width="20"
                            Height="30"
                            Margin="-5,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            d:Visibility="Visible"
                            Background="{StaticResource StatusErrorDarkerBrush}"
                            BorderThickness="0"
                            Command="{Binding DisconnectPulsoidCommand}"
                            Cursor="Hand"
                            Style="{DynamicResource DisconnectPulsoid}"
                            Visibility="{Binding Pulsoid.AuthConnected, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <Grid>
                                <Image
                                    Width="15"
                                    Height="15"
                                    RenderOptions.BitmapScalingMode="Linear"
                                    Source="/Img/Icons/Cross_v1_ico.png" />
                            </Grid>
                        </Button>
                        <Border Visibility="{Binding Modules.Pulsoid.PulsoidAccessError, Converter={StaticResource InverseBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock
                                Margin="5,0,0,0"
                                VerticalAlignment="Center"
                                Effect="{StaticResource TextBlockShadowEffect}"
                                FontFamily="Comfortaa Light"
                                FontSize="12"
                                FontWeight="Bold"
                                Foreground="LightSeaGreen"
                                RenderOptions.BitmapScalingMode="NearestNeighbor"
                                Text="No device connected"
                                Visibility="{Binding Modules.Pulsoid.PulsoidDeviceOnline, Converter={StaticResource InverseBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}" />
                        </Border>
                        <TextBlock
                            Width="400"
                            Padding="10,4,0,4"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="14"
                            Foreground="PaleVioletRed"
                            Text="{Binding Modules.Pulsoid.PulsoidAccessErrorTxt, UpdateSourceTrigger=PropertyChanged}"
                            TextWrapping="Wrap"
                            Visibility="{Binding Modules.Pulsoid.PulsoidAccessError, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5"
                    Opacity="0.8"
                    Visibility="{Binding Pulsoid.AuthConnected, Converter={StaticResource InverseBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                    <StackPanel Margin="5">
                        <TextBlock
                            x:Name="ManualPulsoidAuthBtn"
                            Padding="0,0,9,0"
                            VerticalAlignment="Center"
                            Cursor="Hand"
                            FontFamily="Albert Sans Thin"
                            FontSize="15"
                            Foreground="{StaticResource TextLabelBrush}"
                            MouseUp="ManualPulsoidAuthBtn_MouseUp"
                            Text="Manual web authorization" />
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">

                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="2,0,0,2"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Example: ❤️ 88 bpm. Gives a little heart icon as a prefix." />
                        <CheckBox
                            x:Name="MagicHeartRateICON_checkbox"
                            Margin="2,0"
                            IsChecked="{Binding Modules.Pulsoid.Settings.MagicHeartIconPrefix, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Show prefix icon in front of the heart rate
                        </CheckBox>
                    </StackPanel>
                </Border>

                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5"
                    Visibility="{Binding Modules.Pulsoid.Settings.MagicHeartIconPrefix, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="2,0,0,4"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Cycle through different heart symbols (❤️, 💖, 💗, etc.) every time your heart rate updates." />
                        <CheckBox
                            x:Name="MagicHeartRateIcons_checkbox"
                            Margin="2,0"
                            IsChecked="{Binding Modules.Pulsoid.Settings.MagicHeartRateIcons, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Auto change heart rate prefix icons
                        </CheckBox>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Padding="2,0,0,0"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="20"
                            Foreground="{StaticResource TextLabelBrush}"
                            Text="Pulsoid ˢᵗᵃᵗˢ" />

                        <TextBlock
                            Width="auto"
                            Margin="2,0"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="You can show stats over a selected time period." />

                        <CheckBox
                            x:Name="EnableStats"
                            Height="20"
                            Margin="4,4"
                            IsChecked="{Binding Modules.Pulsoid.Settings.PulsoidStatsEnabled, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Enable Pulsoid ˢᵗᵃᵗˢ
                        </CheckBox>

                        <StackPanel Visibility="{Binding Modules.Pulsoid.Settings.PulsoidStatsEnabled, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock
                                Width="auto"
                                Margin="4,2"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="13"
                                Foreground="{StaticResource TextLabelBrush}"
                                Text="Select the timeframe you want to show the stats for." />
                            <ComboBox
                                x:Name="PulsoidTimeFrame_combo"
                                Width="100"
                                Height="25"
                                Margin="4,4"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding Modules.Pulsoid.Settings.StatisticsTimeRanges, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Opacity="0.3"
                                SelectedItem="{Binding Modules.Pulsoid.Settings.SelectedStatisticsTimeRange, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>



                            <TextBlock
                                Width="auto"
                                Padding="4,10,0,0"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="13"
                                Foreground="{StaticResource TextLabelBrush}"
                                Text="Pick the stats you want to show that is collected over your selected time frame." />



                            <CheckBox
                                x:Name="ShowCalories"
                                Height="20"
                                Margin="4,4,2,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowCalories, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Show calories
                            </CheckBox>

                            <CheckBox
                                x:Name="AverageHeartRate"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowAverageHeartRate, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Show average heart rate
                            </CheckBox>

                            <CheckBox
                                x:Name="MaximumHeartRate"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowMaximumHeartRate, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Show maximum heart rate
                            </CheckBox>

                            <CheckBox
                                x:Name="MinimumHeartRate"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowMinimumHeartRate, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Show minimum heart rate
                            </CheckBox>



                            <CheckBox
                                x:Name="RecordedTime"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowDuration, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Show recorded time over selected time frame
                            </CheckBox>

                            <CheckBox
                                x:Name="ShowSelectTimeInRecordedTime"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.ShowStatsTimeRange, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}"
                                Visibility="{Binding Modules.Pulsoid.Settings.ShowDuration, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                Show selected time like (24h) frame in recorded time stat
                            </CheckBox>

                            <CheckBox
                                x:Name="HideCurrentHeartRate"
                                Height="20"
                                Margin="4,12,4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.HideCurrentHeartRate, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Hide current heart rate when showing stats
                            </CheckBox>
                            <CheckBox
                                x:Name="PlaceTrendIndicatorBehindStats"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.TrendIndicatorBehindStats, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Place trend indicator behind stats
                            </CheckBox>


                        </StackPanel>





                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Padding="2,0,0,0"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="20"
                            Foreground="{StaticResource TextLabelBrush}"
                            Text="Pulsoid to OSC" />

                        <TextBlock
                            Width="auto"
                            Margin="2,0"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="With Pulsoid to OSC you can send your heart rate to your VRChat Avatar." />

                        <CheckBox
                            x:Name="EnableOSCStats"
                            Height="20"
                            Margin="4,4"
                            IsChecked="{Binding IntegrationSettings.IntgrHeartRate_OSC, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Enable OSC support
                        </CheckBox>

                        <StackPanel Margin="0,10,0,0" Visibility="{Binding IntegrationSettings.IntgrHeartRate_OSC, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <CheckBox
                                x:Name="sentMCBHeartrateInfo_checkbox"
                                Height="20"
                                Margin="4,4"
                                IsChecked="{Binding Modules.Pulsoid.Settings.SentMCBHeartrateInfo, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Send magic info to OSC, like trend indicator and max, min, avg heart rate.
                            </CheckBox>
                            <CheckBox
                                x:Name="sentMCBHeartrateInfoLegacy_checkbox"
                                Height="20"
                                Margin="4,2"
                                IsChecked="{Binding Modules.Pulsoid.Settings.SentMCBHeartrateInfoLegacy, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}"
                                Visibility="{Binding Modules.Pulsoid.Settings.SentMCBHeartrateInfo, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                Send magic info as legacy values to OSC
                            </CheckBox>

                            <CheckBox
                                x:Name="SmoothOSCHeartRate_checkbox"
                                Height="20"
                                Margin="4,10,4,0"
                                IsChecked="{Binding Modules.Pulsoid.Settings.SmoothOSCHeartRate, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Smooth heart rate to OSC
                            </CheckBox>

                            <StackPanel Margin="4,4,0,0" Visibility="{Binding Modules.Pulsoid.Settings.SmoothOSCHeartRate, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                <StackPanel
                                    Margin="0,5,0,5"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="Albert Sans Thin"
                                        FontSize="15"
                                        Foreground="{StaticResource TextLabelBrush}"
                                        Text="Use last" />
                                    <TextBox
                                        x:Name="SmoothOSCHeartRate_INT"
                                        Width="auto"
                                        MinWidth="20"
                                        Margin="5,0"
                                        Padding="3,0"
                                        VerticalAlignment="Center"
                                        Background="{StaticResource InputBackgroundBrush}"
                                        BorderThickness="0"
                                        FontSize="15"
                                        Foreground="{StaticResource TextDarkBrush}"
                                        Text="{Binding Modules.Pulsoid.Settings.SmoothOSCHeartRateTimeSpan, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="Albert Sans Thin"
                                        FontSize="15"
                                        Foreground="{StaticResource TextLabelBrush}"
                                        Text="received heart rate updates from the network to show smooth heart rate changes." />
                                </StackPanel>
                                <TextBlock
                                    Width="auto"
                                    Margin="2,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="13"
                                    FontStyle="Italic"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Opacity="0.5"
                                    Text="By using a heartrate band, you get way more updates from the network than you would get from a watch." />
                            </StackPanel>
                        </StackPanel>





                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5"
                    Visibility="{Binding Modules.Pulsoid.Settings.MagicHeartIconPrefix, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,0"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Example: If below 60 BPM, shows 'sleepy'; if above 100 BPM, shows 'hot'." />
                        <CheckBox
                            x:Name="ShowTemperatureText_checkbox"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.ShowTemperatureText, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Show temperature status
                        </CheckBox>
                        <StackPanel Margin="4,5,0,0" Visibility="{Binding Modules.Pulsoid.Settings.ShowTemperatureText, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Padding="0,0,9,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Lower than" />
                                <TextBox
                                    x:Name="LowTemperatureThreshold"
                                    Width="35"
                                    Height="auto"
                                    Margin="0,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalContentAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.LowTemperatureThreshold, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextAlignment="Center" />
                                <TextBlock
                                    Padding="5,0,5,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="show" />
                                <TextBox
                                    x:Name="LowHeartRateText"
                                    Height="auto"
                                    MinWidth="60"
                                    Padding="3,0"
                                    HorizontalAlignment="Left"
                                    VerticalContentAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.LowHeartRateText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Margin="0,5,0,5" Orientation="Horizontal">
                                <TextBlock
                                    Padding="0,0,5,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Higher than" />
                                <TextBox
                                    x:Name="HighTemperatureThreshold"
                                    Width="35"
                                    Height="auto"
                                    HorizontalAlignment="Left"
                                    VerticalContentAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.HighTemperatureThreshold, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextAlignment="Center" />
                                <TextBlock
                                    Padding="5,0,4,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="show" />
                                <TextBox
                                    x:Name="HighwHeartRateText"
                                    Height="auto"
                                    MinWidth="60"
                                    Margin="0,0,0,0"
                                    Padding="3,0"
                                    HorizontalAlignment="Left"
                                    VerticalContentAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.HighHeartRateText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,0"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Monitors changes in heart rate over time, showing up/down arrows if you accelerate or decelerate quickly." />
                        <CheckBox
                            x:Name="ShowHeartRateTrendIndicator"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.ShowHeartRateTrendIndicator, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Trend suffix indicators ⤴️ ⤵️
                        </CheckBox>
                        <StackPanel Margin="0,6,0,0" Visibility="{Binding Modules.Pulsoid.Settings.ShowHeartRateTrendIndicator, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock
                                Margin="4,0"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="15"
                                Foreground="{StaticResource TextLabelBrush}"
                                Text="Trend symbols" />
                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">

                                <ComboBox
                                    x:Name="PulsoidTrendSymbols"
                                    Width="100"
                                    Height="25"
                                    Margin="4,2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    DisplayMemberPath="CombinedTrendSymbol"
                                    ItemsSource="{Binding Modules.Pulsoid.Settings.PulsoidTrendSymbols, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Opacity="0.3"
                                    SelectedItem="{Binding Modules.Pulsoid.Settings.SelectedPulsoidTrendSymbol, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel
                                Margin="4,5,0,10"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Use last" />
                                <TextBox
                                    x:Name="HeartRateTrendIndicatorSampleRate_INT"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.HeartRateTrendIndicatorSampleRate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="heart rate scans to identify trends." />
                            </StackPanel>


                            <TextBlock
                                Width="auto"
                                Padding="4,0"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="15"
                                Foreground="{StaticResource TextLabelBrush}"
                                Text="Trend sensitivity" />
                            <StackPanel Margin="3,0" Orientation="Horizontal">

                                <TextBlock
                                    Width="auto"
                                    Padding="2,5,5,5"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="13"
                                    Foreground="{StaticResource BorderMutedBrush}"
                                    Text="Quick" />
                                <Slider
                                    x:Name="HeartRateTrendIndicatorSensitivitySlider"
                                    Width="134.5"
                                    Height="12"
                                    Margin="2.2,0,4.5,0"
                                    Maximum="1"
                                    Minimum="0"
                                    Style="{StaticResource CustomSlider}"
                                    Value="{Binding Path=Modules.Pulsoid.Settings.HeartRateTrendIndicatorSensitivity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    Width="auto"
                                    Padding="5,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="13"
                                    Foreground="{StaticResource BorderMutedBrush}"
                                    Text="Slow" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,0"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Applies a moving average over a few seconds so the displayed BPM doesn't jump too wildly." />
                        <CheckBox
                            x:Name="SmoothHeartRate_checkbox"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.SmoothHeartRate, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Smooth heart rate to Chatbox
                        </CheckBox>
                        <StackPanel Margin="4,4,0,0" Visibility="{Binding Modules.Pulsoid.Settings.SmoothHeartRate, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel
                                Margin="0,5,0,5"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Use last" />
                                <TextBox
                                    x:Name="SmoothHeartRate_INT"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.SmoothHeartRateTimeSpan, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="heart rate scans to show smooth heart rate changes." />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,0"
                            VerticalAlignment="Center"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Any heart rate above the max threshold gets compressed into a smaller range to avoid extremely high values" />
                        <CheckBox
                            x:Name="ThrottleHR"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.ThrottleHR, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Enable Heart Rate Throttling
                        </CheckBox>

                        <StackPanel Margin="4,5,0,0" Visibility="{Binding Modules.Pulsoid.Settings.ThrottleHR, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">



                            <StackPanel
                                Margin="0,5,0,5"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">

                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Max Heart Rate:" />
                                <TextBox
                                    x:Name="throttleRangeMin"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.ThrottleHRMax, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="BPM" />

                            </StackPanel>

                            <StackPanel
                                Margin="0,5,0,5"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Max Overflow:" />

                                <TextBox
                                    x:Name="throttleHRMax"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.ThrottleMaxAdditional, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="BPM" />
                            </StackPanel>
                            <TextBlock
                                Width="auto"
                                Margin="0,0,0,5"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="13"
                                FontStyle="Italic"
                                Foreground="{StaticResource TextLabelBrush}"
                                Opacity="0.5"
                                Text="Max Overflow adds a small extra range above the cutoff to gently scale heart-rate spikes instead of cutting them off abruptly." />


                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,2,0,3"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Applies a constant offset to your measured BPM. Useful if your sensor is known to be +5 BPM off. use - to remove BPM" />
                        <CheckBox
                            x:Name="ApplyHeartRateAdjustment_checkbox"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.ApplyHeartRateAdjustment, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Heart rate adjustment
                        </CheckBox>

                        <StackPanel Margin="4,5,0,0" Visibility="{Binding Modules.Pulsoid.Settings.ApplyHeartRateAdjustment, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel
                                Margin="0,5,0,5"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Adjust heart-rate by" />
                                <TextBox
                                    x:Name="HeartRateAdjustment"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.HeartRateAdjustment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="BPM" />
                            </StackPanel>


                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,2"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Marks your heart rate as 'offline' if it stops updating for too long." />
                        <CheckBox
                            x:Name="EnableHeartRateOfflineCheck_checkbox"
                            Height="22"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.EnableHeartRateOfflineCheck, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Check for offline heart rate
                        </CheckBox>
                        <StackPanel Margin="4,5,0,0" Visibility="{Binding Modules.Pulsoid.Settings.EnableHeartRateOfflineCheck, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel
                                Margin="0,5,0,0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="If the heart rate is unchanged for" />
                                <TextBox
                                    x:Name="UnchangedHeartRateLimit_INT"
                                    Width="auto"
                                    MinWidth="20"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.UnchangedHeartRateTimeoutInSec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="seconds, then the heart rate is considered offline." />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,0"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="Adds 'bpm' after the heart rate, e.g. '85 bpm' instead of just '85'." />
                        <CheckBox
                            x:Name="ShowBPMSuffix_checkbox"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.ShowBPMSuffix, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Show BPM suffix
                        </CheckBox>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,2"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="If disabled, sends only new style parameters (/avatar/parameters/HR, etc.) for a simpler, faster update." />
                        <CheckBox
                            x:Name="DisableLegacyOSCSupport_checkbox"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.DisableLegacySupport, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Disable legacy OSC support (improved performance)
                        </CheckBox>
                    </StackPanel>
                </Border>
                <Border
                    Margin="0,0,0,5"
                    Background="{StaticResource SurfaceCardBrush}"
                    CornerRadius="5,0,0,5">
                    <StackPanel Margin="5">
                        <TextBlock
                            Width="auto"
                            Margin="4,3,0,2"
                            FontFamily="Albert Sans Thin"
                            FontSize="13"
                            FontStyle="Italic"
                            Foreground="{StaticResource TextLabelBrush}"
                            Opacity="0.5"
                            Text="If checked, you'll see a short label (like 'Heart Rate:') before the BPM." />
                        <CheckBox
                            x:Name="HeartRateTitle_checkbox"
                            Height="20"
                            Margin="4,2"
                            IsChecked="{Binding Modules.Pulsoid.Settings.HeartRateTitle, Mode=TwoWay}"
                            Style="{DynamicResource SettingsCheckbox}">
                            Show heart-rate title
                        </CheckBox>

                        <StackPanel Margin="0,10,0,0" Visibility="{Binding Modules.Pulsoid.Settings.HeartRateTitle, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock
                                Margin="4,0"
                                VerticalAlignment="Center"
                                FontFamily="Albert Sans Thin"
                                FontSize="15"
                                Foreground="{StaticResource TextLabelBrush}"
                                Text="The title is shown above/before the heart rate." />
                            <StackPanel
                                Margin="4,5,0,5"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Albert Sans Thin"
                                    FontSize="15"
                                    Foreground="{StaticResource TextLabelBrush}"
                                    Text="Show off as:" />
                                <TextBox
                                    x:Name="HeartRateTitleCustom"
                                    Width="auto"
                                    MinWidth="50"
                                    Margin="5,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource InputBackgroundBrush}"
                                    BorderThickness="0"
                                    FontSize="15"
                                    Foreground="{StaticResource TextDarkBrush}"
                                    Text="{Binding Modules.Pulsoid.Settings.CurrentHeartRateTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <CheckBox
                                x:Name="HeartRateTitleStackMethod_checkbox"
                                Height="20"
                                Margin="4,3"
                                IsChecked="{Binding Modules.Pulsoid.Settings.SeparateTitleWithEnter, Mode=TwoWay}"
                                Style="{DynamicResource SettingsCheckbox}">
                                Seperate with enter
                            </CheckBox>

                        </StackPanel>

                    </StackPanel>
                </Border>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>