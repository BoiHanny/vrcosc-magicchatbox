<UserControl
    x:Class="vrcosc_magicchatbox.UI.Pages.Options.StatusSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:vrcosc_magicchatbox.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:ViewModel,
                                     IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UI/Resources/SharedConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0,5,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.Background>
                            <LinearGradientBrush SpreadMethod="Pad" StartPoint="1,1" EndPoint="0,0">
                                <GradientStop Color="{StaticResource SurfaceGradientStartColor}" />
                                <GradientStop Offset="1" Color="{StaticResource SurfaceGradientEndColor}" />
                            </LinearGradientBrush>
                        </Grid.Background>
                        <Grid Grid.Row="0">
                            <ToggleButton
                                Width="Auto"
                                Height="30"
                                HorizontalAlignment="Left"
                                FontSize="15"
                                FontWeight="Bold"
                                Foreground="{StaticResource TextSectionHeaderBrush}"
                                IsChecked="{Binding AppSettings.Settings_Status, Mode=TwoWay}"
                                Style="{StaticResource ExpandCollapseToggleButtonStyle}"
                                Tag="Status options"
                                TextOptions.TextRenderingMode="ClearType" />

                        </Grid>


                        <Grid
                            x:Name="Settings_Status_Grid"
                            Grid.Row="1"
                            Height="Auto"
                            Margin="34,10,0,0"
                            Visibility="{Binding AppSettings.Settings_Status, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel Margin="5">

                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5">

                                    <CheckBox
                                        x:Name="StatusIconPrefix_checkbox"
                                        Margin="5"
                                        IsChecked="{Binding AppSettings.PrefixIconStatus, Mode=TwoWay}"
                                        Style="{DynamicResource SettingsCheckbox}">
                                        Add 💬 as prefix for the personal status
                                    </CheckBox>
                                </Border>
                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5">

                                    <StackPanel>
                                        <CheckBox
                                            x:Name="EnableEmojiShuffle_checkbox"
                                            Margin="5"
                                            IsChecked="{Binding AppSettings.EnableEmojiShuffle, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}">
                                            Enable custom prefix icon shuffle
                                        </CheckBox>
                                        <StackPanel Visibility="{Binding AppSettings.EnableEmojiShuffle, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                            <TextBlock
                                                Width="auto"
                                                Padding="4,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="13"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="Make sure to add the emojis you want to cycle through in the text box below, separated by a space a comma." />
                                            <TextBox
                                                MinWidth="50"
                                                MaxWidth="630"
                                                Margin="4,3,0,10"
                                                Padding="5,0,5,0"
                                                HorizontalAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                FontSize="15"
                                                Text="{Binding Emojis.EmojiListString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBlock
                                                Width="auto"
                                                Padding="4,5,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="13"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="Here you can add your custom emojis to cycle through." />
                                            <StackPanel Orientation="Horizontal">

                                                <TextBox
                                                    x:Name="EmojiNew"
                                                    Width="auto"
                                                    Height="30"
                                                    MinWidth="25"
                                                    Margin="5"
                                                    Padding="5,0,5,0"
                                                    HorizontalAlignment="Left"
                                                    VerticalContentAlignment="Center"
                                                    FontSize="15"
                                                    PreviewKeyUp="EmojiNew_PreviewKeyUp"
                                                    Text="" />
                                                <Button
                                                    x:Name="AddEmojiButton"
                                                    Width="auto"
                                                    Height="30"
                                                    Padding="5"
                                                    Click="AddEmojiButton_Click"
                                                    Style="{StaticResource Status_Button_style}">
                                                    <Button.ContentTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <Image
                                                                    Width="21"
                                                                    Height="18"
                                                                    Margin="0,0,3,0"
                                                                    RenderOptions.BitmapScalingMode="HighQuality"
                                                                    Source="/Img/Icons/Add.png" />
                                                                <TextBlock
                                                                    Margin="0,-2,5,0"
                                                                    VerticalAlignment="Center"
                                                                    FontSize="14"
                                                                    Text="Add emoji to cycle" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </Button.ContentTemplate>
                                                </Button>
                                            </StackPanel>
                                        </StackPanel>







                                    </StackPanel>
                                </Border>

                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5">
                                    <CheckBox
                                        x:Name="CycleStatus_checkbox"
                                        Margin="5"
                                        IsChecked="{Binding AppSettings.CycleStatus, Mode=TwoWay}"
                                        Style="{DynamicResource SettingsCheckbox}">
                                        Cycle status items that are marked with 💛
                                    </CheckBox>
                                </Border>
                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5"
                                    Visibility="{Binding AppSettings.CycleStatus, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">

                                    <CheckBox
                                        x:Name="IsRandomCycling_checkbox"
                                        Margin="5"
                                        IsChecked="{Binding AppSettings.IsRandomCycling, Mode=TwoWay}"
                                        Style="{DynamicResource SettingsCheckbox}">
                                        Cycle status items in random order
                                    </CheckBox>


                                </Border>

                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5"
                                    Visibility="{Binding AppSettings.CycleStatus, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                    <StackPanel Margin="5" Orientation="Horizontal">
                                        <TextBlock
                                            Padding="0,0,9,0"
                                            VerticalAlignment="Center"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="15"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Text="Cycle to the next every" />
                                        <TextBox
                                            x:Name="SwitchStatusInterval"
                                            Width="auto"
                                            Height="auto"
                                            Margin="0,0,0,0"
                                            Padding="5,0"
                                            HorizontalAlignment="Left"
                                            VerticalContentAlignment="Center"
                                            FontSize="15"
                                            Text="{Binding AppSettings.SwitchStatusInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            TextAlignment="Center" />
                                        <TextBlock
                                            Padding="5,0,5,0"
                                            VerticalAlignment="Center"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="15"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Text="seconds" />

                                    </StackPanel>

                                </Border>

                                <Border
                                    Margin="0,0,0,5"
                                    Background="{StaticResource SurfaceCardBrush}"
                                    CornerRadius="5,0,0,5">
                                    <StackPanel Margin="5">
                                        <TextBlock
                                            Padding="2,0,0,0"
                                            VerticalAlignment="Center"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="20"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Text="AFK Detection" />

                                        <TextBlock
                                            Width="auto"
                                            Padding="2,3,0,0"
                                            VerticalAlignment="Center"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="13"
                                            FontStyle="Italic"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Opacity="0.5"
                                            Text="You can show a custom message when you are AFK" />

                                        <CheckBox
                                            x:Name="EnableAFKDetectCheck"
                                            Margin="4,5,4,4"
                                            IsChecked="{Binding Afk.Settings.EnableAfkDetection, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}">
                                            Enable AFK detection
                                        </CheckBox>

                                        <CheckBox
                                            Margin="4,5,4,4"
                                            IsChecked="{Binding Afk.Settings.ActivateInVR, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}">
                                            Enable When in VR
                                        </CheckBox>


                                        <StackPanel Margin="5" Orientation="Horizontal">
                                            <TextBlock
                                                Padding="0,0,4,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="15"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="Timeout time in" />
                                            <TextBox
                                                x:Name="TimeoutTime"
                                                Width="auto"
                                                Height="auto"
                                                Margin="0,0,0,0"
                                                Padding="5,0"
                                                HorizontalAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                FontSize="15"
                                                Text="{Binding Afk.Settings.AfkTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                TextAlignment="Center" />

                                            <TextBlock
                                                Padding="4,0,2,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="15"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="seconds" />

                                            <TextBlock
                                                Margin="0,-5,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="12"
                                                FontStyle="Italic"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="{Binding Afk.FriendlyTimeoutTime, UpdateSourceTrigger=PropertyChanged}" />

                                        </StackPanel>

                                        <CheckBox
                                            Margin="4,5,4,4"
                                            IsChecked="{Binding Afk.Settings.ShowPrefixIcon, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}">
                                            Show prefix icon
                                        </CheckBox>

                                        <CheckBox
                                            Margin="4,5,4,4"
                                            IsChecked="{Binding Afk.Settings.ShowAFKTime, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}">
                                            Show how long you are AFK
                                        </CheckBox>
                                        <CheckBox
                                            Margin="4,5,4,4"
                                            IsChecked="{Binding Afk.Settings.UseSmallLettersForDuration, Mode=TwoWay}"
                                            Style="{DynamicResource SettingsCheckbox}"
                                            Visibility="{Binding Afk.Settings.ShowAFKTime, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">
                                            Use small letters for duration suffix
                                        </CheckBox>
                                        <TextBlock
                                            Padding="4,5,9,0"
                                            VerticalAlignment="Center"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="15"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Text="AFK style" />
                                        <StackPanel Margin="4,0,0,0" Orientation="Horizontal">

                                            <TextBox
                                                Width="auto"
                                                Margin="0,3,0,0"
                                                Padding="5,0,5,0"
                                                HorizontalAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                FontSize="15"
                                                Text="{Binding Afk.Settings.AfkPrefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Visibility="{Binding Afk.Settings.ShowPrefixIcon, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBox
                                                Width="auto"
                                                Margin="5,3,0,0"
                                                Padding="5,0,5,0"
                                                HorizontalAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                FontSize="15"
                                                Text="{Binding Afk.Settings.AfkMessageForTimeStamp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Visibility="{Binding Afk.Settings.ShowAFKTime, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}" />
                                            <TextBox
                                                Width="auto"
                                                Margin="4,3,0,0"
                                                Padding="5,0,5,0"
                                                HorizontalAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                FontSize="15"
                                                Text="{Binding Afk.Settings.AfkMessageWithoutTimeStamp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Visibility="{Binding Afk.Settings.ShowAFKTime, Converter={StaticResource InverseBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <TextBlock
                                            Margin="4,2,0,0"
                                            FontFamily="Albert Sans Thin"
                                            FontSize="10"
                                            FontStyle="Italic"
                                            Foreground="{StaticResource TextLabelBrush}"
                                            Opacity="0.4"
                                            Text="Use \n for new line" />
                                        <StackPanel Visibility="{Binding AppState.BussyBoysMode, Converter={StaticResource InverseBoolToHiddenConverter}, UpdateSourceTrigger=PropertyChanged}">

                                            <TextBlock
                                                Padding="4,15,9,0"
                                                VerticalAlignment="Center"
                                                FontFamily="Albert Sans Thin"
                                                FontSize="19"
                                                Foreground="{StaticResource TextLabelBrush}"
                                                Text="Bussy Boys (custom counting from date and more)" />

                                            <CheckBox
                                                Margin="4,5,4,4"
                                                IsChecked="{Binding TimeSettings.BussyBoysMultiMODE, Mode=TwoWay}"
                                                Style="{DynamicResource SettingsCheckbox}">
                                                Allow personal AFK + Status
                                            </CheckBox>

                                            <CheckBox
                                                Margin="4,5,4,4"
                                                IsChecked="{Binding TimeSettings.BussyBoysDateEnable, Mode=TwoWay}"
                                                Style="{DynamicResource SettingsCheckbox}">
                                                Start custom counting ↴
                                            </CheckBox>

                                            <DatePicker
                                                Width="250"
                                                Margin="3,5,4,4"
                                                HorizontalAlignment="Left"
                                                BorderThickness="0"
                                                IsTodayHighlighted="True"
                                                Opacity="0.5"
                                                SelectedDate="{Binding TimeSettings.BussyBoysDate, Mode=TwoWay}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                        </Grid>
                    </Grid>
</UserControl>
